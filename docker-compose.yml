version: "3.9"

services:
  hk-parser:
    build: .
    image: hk-parser-service:latest
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=info
      - STORAGE_ROOT=/app/storage
    volumes:
      - ./logs:/app/logs
      - ./storage:/app/storage
